# Kitchen-ansible demo - webserver

This repository contains a simple playbook that installs caddy webserver and 
deploys a simple static web page.

Test suites run and verify the results of the playbook.

